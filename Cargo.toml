[package]
name = "cruzante"
version = "0.1.0"
edition = "2021"
description = "A constructor that rewrites the plan where the river kills it."
license = "MIT"
publish = false
repository = "https://github.com/PascualMacana/cruzante"

[[bin]]
name = "cruzante"
path = "src/main.rs"
